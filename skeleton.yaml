models:
  Post:
    attributes:
      title: string:400
      content: longtext
      published_at: nullable timestamp
      archived_at: nullable timestamp
      draft: boolean
    casts:
      datetime: published_at archived_at
      boolean: draft